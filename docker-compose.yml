services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: derrepente-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-prod}
      - SUPABASE_HOST=${SUPABASE_HOST:-aws-0-sa-east-1.pooler.supabase.com}
      - SUPABASE_PORT=${SUPABASE_PORT:-6543}
      - SUPABASE_DB=${SUPABASE_DB:-postgres}
      - SUPABASE_USER=${SUPABASE_USER:-postgres.hydxnouyyioyxvemboqi}
      - SUPABASE_PASSWORD=${SUPABASE_PASSWORD:-61544196Mts}
      - WEATHER_API_KEY=${WEATHER_API_KEY:-021c05a00ab24cfb88b204516262509}
      - WEATHER_API_URL=${WEATHER_API_URL:-https://api.weatherapi.com/v1/forecast.json}
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: derrepente-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

networks:
  default:
    name: derrepente-network
