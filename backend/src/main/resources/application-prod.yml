spring:
  datasource:
    url: jdbc:postgresql://${SUPABASE_HOST:db.hydxnouyyioyxvemboqi.supabase.co}:${SUPABASE_PORT:5432}/${SUPABASE_DB:postgres}?sslmode=require&prepareThreshold=0
    driver-class-name: org.postgresql.Driver
    username: ${SUPABASE_USER:postgres}
    password: ${SUPABASE_PASSWORD:61544196Mts}
    hikari:
      initialization-fail-timeout: 0
      maximum-pool-size: 5
      minimum-idle: 1
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
