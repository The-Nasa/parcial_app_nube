# Spec Agent 02: Especificación del Agente Frontend React

**Rol:** Senior React UI/UX Engineer Agent  
**Entradas:** `spec/frontend/*.md`, DTOs de `spec/backend/04_rest_api_endpoints.md`.  
**Entregable:** Proyecto React + Vite en la carpeta `frontend/`.

## Instrucciones del Agente Frontend:
1. Crear app React + Vite.
2. Implementar los componentes `DateSelector`, `WeatherCard`, `PredictionCard`, `SafetyBanner` y `HistoryChart`.
3. Aplicar sistema de diseño HSL (tema oscuro selva/catarata Tingo María).
4. Crear `frontend/Dockerfile` servido mediante Nginx web server.
